
// Stadium types
export interface Stadium {
  id: string;
  name: string;
  slug: string;
  city: string;
  capacity: number;
  image: string;
  address: string;
  opened: number;
}

export default Stadium;
